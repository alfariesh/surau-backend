# IDE
.idea
.vscode

# Config
.env
.env.production
.env.local
.env.*.local

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
bin/
# Local build output of `go build ./cmd/app` (anchored: cmd/app stays tracked)
/app

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/
__pycache__/
scripts/__pycache__/
tmp/
.wrangler/

coverage.txt

# Temporary directories
temp-pageIndex/
temp-langextract/
temp-pageIndex-condb/
temp-books-enriched/
.playwright-mcp/
.gstack/
