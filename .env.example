# App
APP_NAME=surau-backend
APP_VERSION=1.0.0
# Deployment environment surfaced by GET /version (dev/prod).
APP_ENV=dev
# HTTP settings
HTTP_PORT=8080
HTTP_USE_PREFORK_MODE=false
# Client IP resolution behind a reverse proxy. The proxy header is honored only
# for requests from a trusted proxy; leave HTTP_TRUSTED_PROXIES empty when the
# app is exposed directly so client-supplied headers cannot spoof the IP (this
# keeps rate limiting and audit logs accurate). Example: 172.16.0.0/12
HTTP_PROXY_HEADER=X-Real-IP
HTTP_TRUSTED_PROXIES=
# Max accepted request body in bytes (default 4 MiB). Editorial page drafts are
# additionally capped at 300k characters by request validation.
HTTP_BODY_LIMIT_BYTES=4194304
# Gzip/deflate/brotli response compression (negotiated via Accept-Encoding).
HTTP_COMPRESSION_ENABLED=true
# Comma-separated browser origins allowed to call the API cross-origin
# (scheme://host[:port], no path) or *. Leave empty to disable CORS entirely
# (same-origin clients and native mobile apps only).
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3005
# Security response headers (X-Frame-Options, X-Content-Type-Options, ...).
SECURITY_HEADERS_ENABLED=true
# Strict-Transport-Security max-age in seconds; keep 0 unless serving HTTPS.
SECURITY_HSTS_SECONDS=0
# Realtime collaborative editing (Yjs/Hocuspocus sidecar, see collab-server/
# and docs/collab.md). When enabled, the app exposes /internal/collab
# endpoints guarded by the A-2 collab-server principal. COLLAB_SERVICE_TOKEN is
# a one-release legacy T1 bridge only; new deployments issue a scoped token and
# put it in COLLAB_SERVICE_TOKEN_FILE on the sidecar.
COLLAB_ENABLED=false
COLLAB_SERVICE_TOKEN=
# Durable /internal request audit is fixed at 90 days.
SERVICE_IDENTITY_AUDIT_RETENTION=2160h
SERVICE_IDENTITY_CLEANUP_ENABLED=true
SERVICE_IDENTITY_CLEANUP_INTERVAL=24h
# Logger
LOG_LEVEL=debug
# PG. Pool sizing: ~10 fits a small VPS; raise with real load testing.
PG_POOL_MAX=10
PG_MAX_CONN_LIFETIME=1h
PG_MAX_CONN_IDLE_TIME=30m
PG_URL=postgres://user:myAwEsOm3pa55@w0rd@localhost:5432/db
# Dedicated operator/job credentials. Owner fallback requires the explicit
# one-release ALLOW_LEGACY_DB_CREDENTIALS=true overlap flag.
IMPORTER_PG_URL=
LANGEXTRACT_PG_URL=
ALLOW_LEGACY_DB_CREDENTIALS=false
# Optional scoped HTTP job credentials; *_FILE wins in production.
RAG_EVAL_SERVICE_TOKEN=
RAG_EVAL_SERVICE_TOKEN_FILE=
SURAU_ENRICHMENT_SERVICE_TOKEN=
SURAU_ENRICHMENT_SERVICE_TOKEN_FILE=
# Postgres server tuning (F1-G) — values are per-host, defaults in the
# compose file fit a 2GB VPS; see docs/deploy-vps.md §Tuning Postgres.
# Changing any of these needs a manual `docker compose up -d db` (brief
# restart) then `pgbackrest check` on prod-style hosts.
#PG_SHARED_BUFFERS=256MB
#PG_EFFECTIVE_CACHE_SIZE=1GB
#PG_WORK_MEM=6MB
#PG_MAINTENANCE_WORK_MEM=128MB
#PG_MAX_CONNECTIONS=50
#PG_LOG_MIN_DURATION=200ms
# Metrics
METRICS_ENABLED=true
# Swagger
SWAGGER_ENABLED=true
# JWT
JWT_TOKEN_EXPIRY=24h
# Access tokens are short-lived; clients renew them via POST /auth/refresh.
JWT_ACCESS_TOKEN_EXPIRY=15m
JWT_REFRESH_TOKEN_EXPIRY=336h
# Local fallback. Production uses JWT_KEYSET_FILE for no-restart rotation.
JWT_SECRET=dev-secret-change-me-32-bytes-minimum
JWT_KEYSET_FILE=
JWT_ISSUER=surau-backend
JWT_AUDIENCE=surau-api

# MFA / TOTP (A-3): step-up freshness, enrollment grace for mandated roles
# (admin), challenge TTLs, and the secret-at-rest key (empty = derive from
# JWT_SECRET; set a dedicated 32+ byte key in prod so JWT rotation cannot
# orphan enrolled authenticators).
MFA_CHALLENGE_TTL=5m
# Production must use a dedicated stable value before JWT key rotation.
MFA_ENCRYPTION_KEY=
MFA_ENROLLMENT_GRACE=168h
MFA_RESET_TTL=15m
MFA_STEP_UP_TTL=10m
MFA_TOTP_ISSUER=Surau

# Progressive login lockout (per email key, escalating backoff)
AUTH_LOCKOUT_ENABLED=true
AUTH_LOCKOUT_THRESHOLD=5
AUTH_LOCKOUT_BASE_DURATION=1m
AUTH_LOCKOUT_FACTOR=15
AUTH_LOCKOUT_MAX_DURATION=1h
# Periodic cleanup of expired auth rows (rate limits, tokens, sessions)
AUTH_CLEANUP_ENABLED=true
AUTH_CLEANUP_INTERVAL=6h
AUTH_CLEANUP_TOKEN_RETENTION=720h
AUTH_CLEANUP_SESSION_RETENTION=720h
# 0 keeps audit logs forever
AUTH_CLEANUP_AUDIT_RETENTION=0
# Admin alerting for high-signal security events (refresh-token reuse).
# When AUTH_ALERT_RECIPIENTS is empty, all admin-role users are emailed.
AUTH_ALERT_ENABLED=false
AUTH_ALERT_INTERVAL=5m
AUTH_ALERT_RECIPIENTS=
# Auth DB-backed rate limits
AUTH_RATE_LIMIT_ENABLED=true
AUTH_RATE_LIMIT_LOGIN_EMAIL_MAX=5
AUTH_RATE_LIMIT_LOGIN_EMAIL_WINDOW=5m
AUTH_RATE_LIMIT_LOGIN_IP_MAX=30
AUTH_RATE_LIMIT_LOGIN_IP_WINDOW=5m
AUTH_RATE_LIMIT_REGISTER_EMAIL_MAX=3
AUTH_RATE_LIMIT_REGISTER_EMAIL_WINDOW=1h
AUTH_RATE_LIMIT_REGISTER_IP_MAX=10
AUTH_RATE_LIMIT_REGISTER_IP_WINDOW=1h
AUTH_RATE_LIMIT_FORGOT_PASSWORD_EMAIL_MAX=3
AUTH_RATE_LIMIT_FORGOT_PASSWORD_EMAIL_WINDOW=1h
AUTH_RATE_LIMIT_FORGOT_PASSWORD_IP_MAX=20
AUTH_RATE_LIMIT_FORGOT_PASSWORD_IP_WINDOW=1h
AUTH_RATE_LIMIT_RESEND_VERIFICATION_EMAIL_MAX=3
AUTH_RATE_LIMIT_RESEND_VERIFICATION_EMAIL_WINDOW=1h
AUTH_RATE_LIMIT_RESEND_VERIFICATION_IP_MAX=20
AUTH_RATE_LIMIT_RESEND_VERIFICATION_IP_WINDOW=1h
AUTH_RATE_LIMIT_RESET_PASSWORD_TOKEN_MAX=5
AUTH_RATE_LIMIT_RESET_PASSWORD_TOKEN_WINDOW=15m
AUTH_RATE_LIMIT_RESET_PASSWORD_IP_MAX=30
AUTH_RATE_LIMIT_RESET_PASSWORD_IP_WINDOW=15m
AUTH_RATE_LIMIT_CHANGE_PASSWORD_USER_MAX=5
AUTH_RATE_LIMIT_CHANGE_PASSWORD_USER_WINDOW=5m
AUTH_RATE_LIMIT_CHANGE_PASSWORD_IP_MAX=30
AUTH_RATE_LIMIT_CHANGE_PASSWORD_IP_WINDOW=5m
AUTH_RATE_LIMIT_CHANGE_EMAIL_USER_MAX=3
AUTH_RATE_LIMIT_CHANGE_EMAIL_USER_WINDOW=1h
AUTH_RATE_LIMIT_CHANGE_EMAIL_IP_MAX=10
AUTH_RATE_LIMIT_CHANGE_EMAIL_IP_WINDOW=1h
AUTH_RATE_LIMIT_CHANGE_EMAIL_TOKEN_MAX=5
AUTH_RATE_LIMIT_CHANGE_EMAIL_TOKEN_WINDOW=15m
AUTH_RATE_LIMIT_DELETE_ACCOUNT_USER_MAX=3
AUTH_RATE_LIMIT_DELETE_ACCOUNT_USER_WINDOW=1h
AUTH_RATE_LIMIT_DELETE_ACCOUNT_IP_MAX=10
AUTH_RATE_LIMIT_DELETE_ACCOUNT_IP_WINDOW=1h

# MFA rate limits (A-3)
AUTH_RATE_LIMIT_MFA_RESET_EMAIL_MAX=3
AUTH_RATE_LIMIT_MFA_RESET_EMAIL_WINDOW=1h
AUTH_RATE_LIMIT_MFA_RESET_IP_MAX=10
AUTH_RATE_LIMIT_MFA_RESET_IP_WINDOW=1h
AUTH_RATE_LIMIT_MFA_STEP_UP_IP_MAX=60
AUTH_RATE_LIMIT_MFA_STEP_UP_IP_WINDOW=15m
AUTH_RATE_LIMIT_MFA_STEP_UP_USER_MAX=10
AUTH_RATE_LIMIT_MFA_STEP_UP_USER_WINDOW=5m
AUTH_RATE_LIMIT_MFA_VERIFY_IP_MAX=30
AUTH_RATE_LIMIT_MFA_VERIFY_IP_WINDOW=15m
AUTH_RATE_LIMIT_MFA_VERIFY_TOKEN_MAX=5
AUTH_RATE_LIMIT_MFA_VERIFY_TOKEN_WINDOW=5m

# Auth security email notifications
AUTH_EMAIL_NOTIFICATIONS_ENABLED=true
AUTH_FAILED_LOGIN_EMAIL_COOLDOWN=24h
AUTH_NEW_LOGIN_EMAIL_ENABLED=true
AUTH_FAILED_LOGIN_EMAIL_ENABLED=true
AUTH_PASSWORD_CHANGED_EMAIL_ENABLED=true
AUTH_EMAIL_VERIFIED_EMAIL_ENABLED=true
AUTH_ROLE_CHANGED_EMAIL_ENABLED=true
AUTH_EMAIL_CHANGED_EMAIL_ENABLED=true
AUTH_ACCOUNT_DELETED_EMAIL_ENABLED=true
# Email. Use "log" locally to print verification/reset links to app logs.
EMAIL_DELIVERY_MODE=log
CF_EMAIL_ACCOUNT_ID=dev-cloudflare-account-id
CF_EMAIL_API_TOKEN=dev-cloudflare-api-token
EMAIL_FROM_ADDRESS=noreply@example.com
EMAIL_FROM_NAME=Surau
EMAIL_REPLY_TO=
EMAIL_VERIFY_FRONTEND_URL=http://localhost:3005/verify-email
EMAIL_VERIFICATION_TTL=24h
EMAIL_RESEND_COOLDOWN=1m
PASSWORD_RESET_FRONTEND_URL=http://localhost:3005/reset-password
PASSWORD_RESET_TTL=1h
PASSWORD_RESET_RESEND_COOLDOWN=1m
EMAIL_CHANGE_FRONTEND_URL=http://localhost:3005/change-email
EMAIL_CHANGE_TTL=24h
EMAIL_CHANGE_RESEND_COOLDOWN=1m
EMAIL_UNSUBSCRIBE_FRONTEND_URL=http://localhost:3005/unsubscribe
EMAIL_UNSUBSCRIBE_PUBLIC_URL=http://localhost:8080/v1/email/unsubscribe
EMAIL_CLOUDFLARE_EVENT_POLLING_ENABLED=false
EMAIL_CLOUDFLARE_ZONE_ID=
EMAIL_CLOUDFLARE_ANALYTICS_API_TOKEN=
EMAIL_CLOUDFLARE_EVENT_POLLING_INTERVAL=5m
EMAIL_CLOUDFLARE_EVENT_POLLING_LOOKBACK=30m
EMAIL_CLOUDFLARE_EVENT_POLLING_LIMIT=100
EMAIL_HTTP_TIMEOUT=10s
# Background dispatcher pace for queued transactional/campaign emails. Auth
# emails (verification, reset) are enqueued, so this bounds their latency.
EMAIL_DISPATCH_INTERVAL=15s
EMAIL_DISPATCH_BATCH=20
# OneSignal push notifications. Disabled by default; the REST API key is a SECRET — never commit a
# real value. The App ID is the public OneSignal app id (also embedded in the Android app).
ONESIGNAL_ENABLED=false
ONESIGNAL_APP_ID=7a650cae-1c1e-4b19-a7fe-393c14b894f0
ONESIGNAL_REST_API_KEY=
ONESIGNAL_HTTP_TIMEOUT=10s
ONESIGNAL_REMINDER_INTERVAL=1h
ONESIGNAL_QUIET_HOURS_START_LOCAL=21:00
ONESIGNAL_QUIET_HOURS_END_LOCAL=07:00
# Book RAG
RAG_LLM_BASE_URL=https://ai.sumopod.com/v1
# AI Gateway custom provider example:
# RAG_LLM_BASE_URL=https://gateway.ai.cloudflare.com/v1/CHANGE_ME_ACCOUNT_ID/surau-rag/CHANGE_ME_CUSTOM_PROVIDER_SLUG/v1
RAG_LLM_API_KEY=
RAG_LLM_MODEL=glm-5.1
RAG_LLM_TIMEOUT=45s
RAG_LLM_MAX_TOKENS=1400
RAG_LLM_TEMPERATURE=0.1
RAG_MAX_CONTEXT_PAGES=8
RAG_TREE_FULL_MAX_NODES=450
RAG_TREE_BLOCK_MAX_NODES=120
RAG_TREE_BEAM_SIZE=3
RAG_TREE_MAX_TURNS=6
RAG_TREE_MAX_BLOCKS_PER_TURN=6
SUMMARY_LLM_BASE_URL=https://ai.sumopod.com/v1
SUMMARY_LLM_API_KEY=
SUMMARY_LLM_MODEL=glm-5.1
